############## Throughput/Goodput ####################

if [ $3 == 'true' ]
then
    if [ $1 == 'true' ]
    then
        throughput_output="$(iperf3 -c $5 -t 10 --get-server-output --logfile /root/logfiles/iperf_results.txt)"
        echo "SEE iperf_results.txt FOR OUTPUT" >> /root/logfiles/results.txt
    else
        throughput_output="$(iperf3 -c $5 -t 10 --get-server-output)"
        echo $throughput_output
    fi
fi
